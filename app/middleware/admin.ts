export default defineNuxtRouteMiddleware((to, from) => {
  // Admin role check (stubbed for Phase 1)
  // Phase 2 will implement actual role checking from Firebase token/claims
  // and redirect to /login if not an admin.
  return
})
