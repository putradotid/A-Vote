export default defineNuxtRouteMiddleware((to, from) => {
  // Global auth check (stubbed for Phase 1)
  // Phase 2 will implement actual Firebase Auth checking and redirect
  // logic to /login or /admin/login depending on route.
  return
})
