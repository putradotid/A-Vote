rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Phase 1 stub
    // Full security rules will be implemented in Phase 5
    // Default deny all client access. Admin SDK bypasses this.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
