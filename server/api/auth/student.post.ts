import { verifyToken } from '../../utils/verify-token'

export default defineEventHandler(async (event) => {
  // Phase 1 stub
  // Phase 2 will implement student login logic
  return {
    customToken: 'stub_token'
  }
})
